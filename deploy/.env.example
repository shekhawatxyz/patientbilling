COMPOSE_PROJECT_NAME=billing-dev
PLATFORM_USERNAME=platform_admin@zango.dev
PLATFORM_USER_PASSWORD=Zango@123
PROJECT_NAME=zango_project
POSTGRES_USER=zango
POSTGRES_PASSWORD=zango
POSTGRES_DB=zango
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
REDIS_HOST=redis
REDIS_PORT=6379
PLATFORM_DOMAIN_URL=localhost
CSRF_TRUSTED_ORIGINS=http://localhost:3000
# Generate your own: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=
UPDATE_APPS_ON_STARTUP=true

# Optional — only needed to test the AI agents against a real LLM instead of the
# deterministic local_fake provider. Never commit a real key here.
ANTHROPIC_KEY=
